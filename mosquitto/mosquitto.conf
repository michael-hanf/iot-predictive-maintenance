# Mosquitto MQTT Broker Configuration
# Docker-optimized configuration for local network communication

# Listen on all interfaces at port 1883
listener 1883
protocol mqtt

# Allow anonymous connections (suitable for internal Docker networks)
allow_anonymous true

# Default settings
persistence false
